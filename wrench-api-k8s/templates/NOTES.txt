Chart "{{ .Chart.Name }}" (release "{{ .Release.Name }}") implantado no namespace "{{ .Values.namespace }}".

1. Verifique se os pods subiram:
   kubectl get pods -n {{ .Values.namespace }}

2. A API é exposta pelo Gateway de plataforma {{ .Values.gateway.namespace }}/{{ .Values.gateway.name }} no hostname:
   https://{{ .Values.httpRoute.hostname }}

3. Confira se a rota foi aceita pelo Gateway:
   kubectl get httproute wrench-http-route -n {{ .Values.namespace }} -o yaml

4. Rode os testes de conectividade do chart:
   helm test {{ .Release.Name }} -n {{ .Values.namespace }}
