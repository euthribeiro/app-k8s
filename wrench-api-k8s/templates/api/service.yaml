apiVersion: v1
kind: Service
metadata:
  name: wrench-api-svc
  namespace: {{ .Values.namespace }}
spec:
  type: ClusterIP
  selector:
    app: wrench-api
  ports:
    - name: http
      protocol: TCP
      port: {{ .Values.api.containerPort }}
      targetPort: {{ .Values.api.containerPort }}
